if(typeof jQuery != "undefined"){
  $(function(){
    $('#reviews_validation').html("<%= j(render 'message') %>");

    $('#sites_validation').html("");

    $('#notice_message').html("");
    });
  }
