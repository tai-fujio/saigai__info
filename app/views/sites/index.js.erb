if(typeof jQuery != "undefined"){
  $(function(){
    $("#sites_show").html("<%= j(render 'sites/show',{map:@map,site:@site}) %>")
    
    $("#comments_new").html("<%= j(render 'comments/new',{map:@map,site:@site,comment:@comment}) %>")

    $("#comments_index").html("<%= j(render 'reviews/index',{comments:@comments}) %>")

    $("textarea").val("");
    });
  }
