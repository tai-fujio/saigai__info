if(typeof jQuery != "undefined"){
  $(function(){
    
    $('#notice_message').html("<%= j(render 'error_message') %>");
    
    $('#reviews_validation').html("");

    $("textarea").val("");
    });
  }
