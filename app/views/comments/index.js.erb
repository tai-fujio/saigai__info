if(typeof jQuery != "undefined"){
$(function(){

  $("#comments_index").html("<%= j(render 'comments/index', { comments: @comments,site:@site}) %>")
  $('#comments_validation').html("<%= j(render 'message') %>");

  $("textarea").val("");
  });
}
